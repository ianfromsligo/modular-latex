The longest revival period in the entire dataset, $T=60$, occurs on the
$5 \times 5$ torus --- the smallest torus (aside from $2\times2$ and
$3\times3$) that supports it. This is the single most striking result.
