This work makes three contributions. First, we describe a component
model in which paragraphs, equations, figures and tables are stored as
independently versioned units. Second, we present a build system that
assembles these components into a templated document. Third, we
demonstrate a continuous-integration workflow that compiles the document
on every change.
